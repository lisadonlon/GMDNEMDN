// DEPRECATED: GMDN functionality has been removed from the application.
// This component is kept as a stub to avoid import errors if referenced accidentally.
// Do not use.
import React from 'react';

const GmdnList: React.FC = () => {
  if (process.env.NODE_ENV !== 'production') {
    console.warn('GmdnList is deprecated and should not be used.');
  }
  return null;
};

export default GmdnList;
